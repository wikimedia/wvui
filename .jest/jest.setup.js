/* eslint-disable no-implicit-globals */
// eslint-disable-next-line no-redeclare

// This is the place where we can setup mocks (see https://jestjs.io/docs/en/mock-functions.html)
// for dependencies we're going to use (f.i. jQuery, mw, OOJS),
// so that it will be accessible with for all tests.

/* For instance:
*
const someDependency = {
  fn: jest.fn()
}

global.someDependency = someDependency;
*
* */
